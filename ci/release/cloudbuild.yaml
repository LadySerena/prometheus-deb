steps:
  - name: gcr.io/cloud-builders/gradle
    entrypoint: bash
    args:
      - "./gradlew"
      - "build"
      - "-Ppackage_version=$TAG_NAME"
  - name: bash
    args:
      - 'ls'
      - '-alh'
      - './build/distributions'